package com.regres.pages;

import org.openqa.selenium.WebDriver;

public class ResourcesSearchPage {
    WebDriver driver;
    //add another fields

    public ResourcesSearchPage(WebDriver driver) {
        this.driver = driver;
    }
}
