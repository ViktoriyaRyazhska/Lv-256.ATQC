package taskbook;

import org.junit.Before;
import org.testng.Assert;
import org.testng.annotations.DataProvider;
import org.testng.annotations.Test;

import JUnitTest.TaskBookD;
import home.Url;

public class task88wTest {
//	task88wTest threenum = task88w.task88wTest();
//	String expected = "x = 2, y = 2, z = 2";
//	Assert.assertEquals(expected , threenum.toString());
//	}
@Test
public void testFactorialPositive() {
	task88wTest task88w = new task88wTest();
	String expexted =  "654";;
	int actual = task88w.replaceFirstLast(456);
}


}